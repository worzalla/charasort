// Congregate info
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Selection({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
