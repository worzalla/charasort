// Invisible button that surrounds Member Image and Bio Card, allows member to be preferred in sort
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ClickableMember({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
