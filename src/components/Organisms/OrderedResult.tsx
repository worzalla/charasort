// Displays members and their position
// 3 variants: Large Image (1-3), Small Image (4-50), and Text-Only (51+)
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function OrderedResult({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
