// A file that handles color information in the sorter
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Colors({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
