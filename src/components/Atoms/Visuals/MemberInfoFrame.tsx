// A card frame with stylized color border, contains info
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function MemberInfoFrame({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
