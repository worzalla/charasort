// A small attention-drawing image
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Icon({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
