// Rectangle with background and text that emphasizes the text
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function InfoPlate({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
