// An image or text that displays the source of the sortable content
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function SourceIdentifier({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
