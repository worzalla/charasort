// Allows users to easily find repro to fork (3rd party asset)
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function GithubLink({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
