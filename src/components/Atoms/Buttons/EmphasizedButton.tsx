// A button that draws users' attention and indicates a major action
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function EmphasizedButton({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
