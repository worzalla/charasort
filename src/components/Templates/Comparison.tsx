// In-progress sorting
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Comparison({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
