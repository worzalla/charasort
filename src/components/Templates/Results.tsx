// The results of the sorter
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Results({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
