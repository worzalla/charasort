// Allows the user to determine the data that will be loaded into the sorter
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Selection({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
