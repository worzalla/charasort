// A card that shows an image with instances of Info Plates displaying Name, Group, Title, etc.
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ImageCardLarge({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
