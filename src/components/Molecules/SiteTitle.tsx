// A link that brings the user back to the homepage, also displays basic info about the site
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function SiteTitle({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
