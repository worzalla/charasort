// A card that displays an image and name with a color background
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ImageCardSmall({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
