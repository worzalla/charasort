// A card that displays biographic information
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function MemberInfo({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
