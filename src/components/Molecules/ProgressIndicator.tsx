// A progress bar and text that measures completion
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ProgressIndicator({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
