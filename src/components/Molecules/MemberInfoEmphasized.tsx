// A segment separated from other content by a divider. Grows from bottom up.
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function MemberInfoEmphasized({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}
